@model List<WebMieow.Models.Order>

@{
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Admin - Quản lý đơn hàng</title>
    <link rel="stylesheet" href="~/vendor/bootstrap/css/bootstrap.min.css" />
    <link rel="shortcut icon" href="~/img/yae.gif" type="image/png" />
</head>
<body class="p-4">
    <div class="container">
        <a href="/Shop" class="logo" style="font-weight: bold; font-size: 22px; color: #000; text-decoration: none;">
            ฿ Danh Sách Đơn Hàng ฿
        </a>


        @if (TempData["Message"] != null)
        {
            <div class="alert alert-info">@TempData["Message"]</div>
        }

        <table class="table table-bordered table-striped">
            <thead class="table-dark">
                <tr>
                    <th>Người dùng</th>
                    <th>Email/SDT</th>
                    <th>Loại dịch vụ</th>
                    <th>Mã đơn</th>
                    <th>Trạng thái</th>
                    <th>Mã thẻ</th>
                    <th>Cập nhật</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var order in Model)
                {
                    <tr>
                        <form asp-controller="Shop" asp-action="UpdateOrDeleteOrder" method="post" onsubmit="return confirm('Bạn chắc chắn với thao tác này?');">

                            <input type="hidden" name="orderCode" value="@order.OrderCode" />

                        <td>@order.Username</td>
                        <td>@order.Contact</td>
                        <td>@order.Plan</td>
                        <td>@order.OrderCode</td>

                        <td>
                            <select name="status" class="form-select form-select-sm w-100">
                                <option value="pending" selected="@(order.Status == "pending" ? "selected" : null)">Chờ</option>
                                <option value="success" selected="@(order.Status == "success" ? "selected" : null)">Đã Duyệt</option>
                            </select>
                        </td>

                        <td>
                            @if (!string.IsNullOrEmpty(order.Card))
                            {
                                <input type="text" class="form-control form-control-sm text-success" name="card" value="@order.Card" readonly />
                            }
                            else
                            {
                                <input type="text" class="form-control form-control-sm" name="card" placeholder="Nhập mã thẻ..." />
                            }
                        </td>

                        <td>
                            <div class="d-flex gap-1">
                                <button type="submit" name="action" value="update" class="btn btn-sm btn-primary">Lưu 💾</button>
                                <button type="submit" name="action" value="delete" class="btn btn-sm btn-danger">Xoá 🗑️</button>
                            </div>
                        </td>
                        </form>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</body>
</html>
