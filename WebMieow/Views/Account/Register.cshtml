@{
    Layout = "~/Views/Shared/_LayoutShop.cshtml";
    ViewData["Title"] = "Đăng Ký";
}

<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Đăng Ký Tài Khoản</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background: #f4f6f9;
        }

        .register-container {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .register-box {
            width: 360px;
            background: #fff;
            padding: 25px;
            border-radius: 12px;
            box-shadow: 0 0 12px rgba(0,0,0,0.1);
        }

            .register-box h2 {
                text-align: center;
                margin-bottom: 20px;
                font-weight: 600;
            }

        .form-control {
            padding: 10px;
            font-size: 15px;
        }
    </style>
</head>
<body>

    <div class="register-container">
        <div class="register-box">

            <h2>Đăng Ký</h2>

            @if (ViewBag.Message != null)
            {
                <div class="alert alert-danger text-center">
                    @ViewBag.Message
                </div>
            }

            <form method="post" action="/Account/Register">
                <input type="text" name="username" placeholder="Tên đăng nhập" class="form-control mb-3" required />

                <input type="password" name="password" placeholder="Mật khẩu" class="form-control mb-3" required />

                <input type="password" name="confirmPassword" placeholder="Nhập lại mật khẩu" class="form-control mb-3" required />


                <input type="email" name="email" placeholder="Email" class="form-control mb-3" required />

                <input type="tel" name="phone" placeholder="Số điện thoại" class="form-control mb-3"
                       pattern="[0-9]{9,11}"
                       title="Nhập số điện thoại hợp lệ (9-11 số)" required />
                <input type="text" name="fullName" placeholder="Họ và tên" class="form-control mb-3" required />

                <button type="submit" class="btn btn-success w-100">Đăng Ký</button>
            </form>

            <div class="text-center mt-3">
                <a href="/Account/Login">Đã có tài khoản? Đăng nhập</a>
            </div>
        </div>
    </div>
    @model WebMieow.Models.ProfileViewModel
@* Xoá các dòng dưới nếu không dùng *@
@* @Model.Account.Username
@Model.Account.FullName
@Model.Account.Email
@Model.Account.Phone
@Model.Account.Address *@




@{
    Layout = "~/Views/Shared/_LayoutShop.cshtml";
    ViewData["Title"] = "Thông tin tài khoản";
}

<section id="profile" class="py-5" style="min-height: 80vh;">
    <div class="container">
        <div class="row gx-4">
            <!-- Thông tin cá nhân -->
            <div class="col-lg-5 mb-4">
                <div class="card shadow-sm border-0">
                    <div class="card-header bg-primary text-white">
                        <h5 class="mb-0">Thông tin cá nhân</h5>
                    </div>
                    <div class="card-body">
                        <table class="table table-borderless mb-0">
                            <tr>
                                <th style="width: 40%;">Tên đăng nhập:</th>
                                <td>@Model.Username</td>
                            </tr>
                            @* <tr>
                                <th>Họ tên:</th>
                                <td>@Model.FullName</td>
                            </tr> *@
                            <tr>
                                <th>Email:</th>
                                <td>@Model.Email</td>
                            </tr>
                            <tr>
                                <th>Điện thoại:</th>
                                <td>@Model.Phone</td>
                            </tr>
                            @* <tr>
                                <th>Địa chỉ:</th>
                                <td>@Model.Address</td>
                            </tr> *@
                        </table>
                        @if (Context.Session.GetInt32("IsAdmin") == 1)
                        {
                            <div class="mt-3">
                                <a href="/Shop/Admin" class="btn btn-warning">Quản lý đơn</a>
                            </div>
                            <div class="mt-3">
                                <a href="/Home/Index" class="btn btn-warning">Về Home</a>
                            </div>
                        }
                    </div>
                </div>
            </div>

            <!-- Lịch sử đơn hàng -->
            <div class="col-lg-7">
                <div class="card shadow-sm border-0">
                    <div class="card-header bg-success text-white">
                        <h5 class="mb-0">Lịch sử đơn hàng</h5>
                    </div>
                    <div class="card-body">
                        @if (Model.Orders != null && Model.Orders.Any())
                        {
                            <div class="table-responsive">
                                <table class="table table-sm table-hover table-bordered">
                                    <thead class="table-light">
                                        <tr>
                                            <th>Mã đơn</th>
                                            <th>Loại hàng</th>
                                            <th>Thời gian</th>
                                            <th>Trạng thái</th>
                                            <th>Mã thẻ</th>
                                            <th>Gmail nhận</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (var order in Model.Orders)
                                        {
                                            <tr>
                                                <td>@order.OrderCode</td>
                                                <td>@order.Plan</td>
                                                <td>@order.CreatedAt.ToString("dd/MM/yyyy HH:mm:ss")</td>
                                                <td>@order.Status</td>
                                                <td>@order.Card</td>
                                                <td>@order.Contact</td>
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                            </div>
                        }
                        else
                        {
                            <p class="text-muted">Bạn chưa có đơn hàng nào.</p>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
<script>
  // Chặn chuột phải
  document.addEventListener('contextmenu', e => e.preventDefault());

  // Chặn F12 và Ctrl+Shift+I với thông báo
  document.addEventListener('keydown', function (e) {
    if (e.key === "F12" || (e.ctrlKey && e.shiftKey && e.key === "I")) {
      e.preventDefault();
      alert("Bấm F12 làm gì, định copy à, có làm mới có ăn nha.");
    }
  });
</script>



    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
